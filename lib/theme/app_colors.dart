import 'package:flutter/material.dart';

class AppColors {
  // Login theme integration - Premium Luxury Palette
  static const primaryGold = Color(0xFFC9BB9D);  // Login primary gold
  static const darkBg = Color(0xFF1E1702);       // Login dark background
  static const accentGold = Color(0xFFE2B692);   // Login accent gold
  
  // Existing luxury palette (backward compatible)
  static const background = Color(0xFFF8F7F4); // Cream white background
  static const card = Color(0xFFFFFFFF);       // Pure white cards
  static const primary = Color(0xFFD4AF37);    // Gold primary
  static const accent = Color(0xFFB8860B);     // Gold accent
  static const goldAccent = Color(0xFFB8860B); // Gold accent
  static const black = Color(0xFF0A0A0A);      // Deep black
  static const dark = Color(0xFF1A1A1A);       // Dark surface
  static const textLight = Color(0xFF666666);  // Muted gray
  static const textDark = Color(0xFF0A0A0A);   // Deep black headings
  static const border = Color(0xFFEEEEEE);     // Subtle border
  static const darkSurface = Color(0xFF1A1A1A); 
}
